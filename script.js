const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('nav');menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.getElementById('year').textContent=new Date().getFullYear();const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const canvas=document.getElementById('embers'),ctx=canvas.getContext('2d');let particles=[];function resize(){canvas.width=innerWidth*devicePixelRatio;canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0)}function reset(p){p.x=Math.random()*innerWidth;p.y=innerHeight+Math.random()*100;p.r=.5+Math.random()*1.8;p.v=.2+Math.random()*.8;p.d=(Math.random()-.5)*.4;p.a=.15+Math.random()*.55}function seed(){particles=Array.from({length:Math.min(80,Math.floor(innerWidth/16))},()=>{const p={};reset(p);p.y=Math.random()*innerHeight;return p})}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);for(const p of particles){ctx.beginPath();ctx.fillStyle=`rgba(255,120,25,${p.a})`;ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill();p.y-=p.v;p.x+=p.d;if(p.y<-10)reset(p)}requestAnimationFrame(draw)}addEventListener('resize',()=>{resize();seed()});resize();seed();draw();
